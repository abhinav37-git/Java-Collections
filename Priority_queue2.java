import java.util.*;

class Priority_queue2{

    //Objective priority queue function to create the priority queue from the user
    public staic pqueue2<>(new PriorityQueue<>()){
        Scanner sc = new Scanner(System.in);

        for(int i: pqueue2){
            pqueue2(sc.nextInt());
        }

        sc.close();
        return pqueue2;
    }

    //Main function to accept the input from the console
    public static void main(String args[]){
        PriorityQueue<Integer> pqueue3 = new PriorityQueue<>();

        pqueue3.add("Abhinav");
        public.add("New_keyword");

        Iterator<Integer> itr = pqueue3.iterator();

        while(itr.hasNext()){
            System.out.printlnt("The elements of the priorityQueue are" + itr.Next());
        }
    }
}
